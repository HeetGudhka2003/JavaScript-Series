// Initializing a variable 'score' with a string value "33abc"

let score = "33abc";

// Initializing a variable 'score1' with a value of null

let score1 = null;

// Initializing a variable 'score2' with a boolean value true

let score2 = true;

// Output the data type of the variable 'score' using the typeof operator

console.log(typeof score);

// Convert the string 'score' to a number using the Number() function

let valueInNumber = Number(score);

// Output the data type of the converted 'valueInNumber' variable

console.log(typeof valueInNumber);

// Output the actual numerical value after conversion

console.log(valueInNumber);

// Output the data type of the variable 'score1'

console.log(typeof score1);

// Convert the value of 'score1' (null) to a number

let valueInNumber2 = Number(score1);

// Output the data type of the converted 'valueInNumber2' variable

console.log(typeof valueInNumber2);

// Output the actual numerical value after conversion

console.log(valueInNumber2);

// Output the data type of the variable 'score2'

console.log(typeof score2);

// Convert the boolean 'score2' to a number

let valueInNumber3 = Number(score2);

// Output the data type of the converted 'valueInNumber3' variable

console.log(typeof valueInNumber3);

// Output the actual numerical value after conversion

console.log(valueInNumber3);

// Attempting to use a variable 'isLoggedIn' (not declared in the provided code)

// Assuming it should be 'score2' instead

let booleanIsLoggedIn = Boolean(score2);

// Output the result of converting 'score2' to a boolean using the Boolean() function

console.log(booleanIsLoggedIn);

// Boolean conversion rules:

// 1 => true

// 0 => false

// "" (empty string) => false

// "hitesh" (non-empty string) => true